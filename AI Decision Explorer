
##  **PROJECT 1: AI Decision Explorer**

**File:** `01_ai_decision_explorer/mind_reader.py`
```python
#  AI Decision Explorer - Peeking Inside the AI's Mind
# By Moses Kiprono

class MindReader:
    def __init__(self):
        self.ai_model = None
        self.mental_map = None
        
    def explore_ai_consciousness(self):
        """Explore how AI models make decisions"""
        from sklearn.datasets import load_digits
        from sklearn.ensemble import RandomForestClassifier
        import matplotlib.pyplot as plt
        import numpy as np
        
        print("🧠 Activating AI Mind Reading...")
        
        # Load digital thought patterns
        digital_dreams = load_digits()
        dream_images = digital_dreams.data
        dream_labels = digital_dreams.target
        
        # Train our thought interpreter
        self.ai_model = RandomForestClassifier(n_estimators=100, random_state=42)
        self.ai_model.fit(dream_images, dream_labels)
        
        # Map the AI's focus areas
        self.mental_map = self.ai_model.feature_importances_.reshape(8, 8)
        
        print(" AI Consciousness Mapped!")
        return digital_dreams
        
    def visualize_ai_focus(self, digital_dreams):
        """Visualize what the AI pays attention to"""
        import matplotlib.pyplot as plt
        
        plt.figure(figsize=(15, 5))
        
        # Human perception
        plt.subplot(1, 3, 1)
        plt.imshow(digital_dreams.images[0], cmap='gray')
        plt.title("Human Vision\n(What We See)")
        plt.axis('off')
        
        # AI's mental focus
        plt.subplot(1, 3, 2)
        plt.imshow(self.mental_map, cmap='YlOrRd')
        plt.title("AI's Mind\n(What AI Focuses On)")
        plt.axis('off')
        
        # Combined insight
        plt.subplot(1, 3, 3)
        plt.imshow(digital_dreams.images[0], cmap='gray')
        plt.imshow(self.mental_map, cmap='YlOrRd', alpha=0.6)
        plt.title("Mind Meld\n(Human + AI Perspective)")
        plt.axis('off')
        
        plt.tight_layout()
        plt.show()
        
    def share_insights(self):
        """Share discoveries about AI decision-making"""
        print("\n AI Decision Explorer Insights:")
        print("   • AI focuses on PATTERNS, not pixels")
        print("   • Bright areas = Critical decision points") 
        print("   • AI learns to ignore noise and distractions")
        print("   • Each model develops unique 'attention patterns'")
        print("   • Understanding this helps build better AI systems")

def main():
    print(" Welcome to AI Decision Explorer!")
    print("Mapping the mysterious landscape of AI consciousness...")
    
    explorer = MindReader()
    dreams = explorer.explore_ai_consciousness()
    explorer.visualize_ai_focus(dreams)
    explorer.share_insights()
    
    print("\n🎊 Exploration Complete!")
    print("We've peered into the AI's mind and understood its decision process!")

if __name__ == "__main__":
    main()
